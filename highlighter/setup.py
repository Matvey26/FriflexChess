from setuptools import setup, find_packages

setup(
    name="highlighter",
    version="0.0.0",
    packages=find_packages(),
    description="Библиотека для выделения моментов",
    author="-",
    author_email="-",
    install_requires=[],  # Зависимости (например: ["requests>=2.25.1"])
    python_requires=">=3.8",  # Минимальная версия Python
)